version: 2.1

jobs:
  build:
    docker:
      - image: circleci/node:14
    working_directory: ~/SE-Coursework/src
    steps:
      - checkout
      - run:
          name: Install dependencies
          command: npm install
      - run:
          name: Run unit tests
          command: npm test